---
name: "apply_plan"
pack: "terraform"
description: "Apply plan"
action_ref: "terraform.apply"
ack:
  format: "Applying Terraform Plan..."
  append_url: false
formats:
  - "terraform apply {{planpath}}"
result:
  format: "action completed! {~} {{ execution.result.result[1] }}"
