---
name: "destroy_plan"
pack: "terraform"
description: "Destroy plan"
action_ref: "terraform.destroy"
ack:
  format: "Destroying Stuff Now!"
  append_url: false
formats:
  - "terraform destroy {{planpath}}"
result:
  format: "action completed! {~} {{ execution.result.result[1] }}"
